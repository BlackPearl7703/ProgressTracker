# Workspace Notes

- React + TypeScript + Vite project.
- Use `npm run dev` for local development and `npm run build` for verification.
- Keep the tracker local-first unless a backend is explicitly requested.
